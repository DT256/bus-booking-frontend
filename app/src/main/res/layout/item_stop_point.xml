<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:layout_marginHorizontal="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Nội dung bên trái -->
        <LinearLayout
            android:id="@+id/llTextContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_toStartOf="@+id/cbSelect"
            android:layout_marginEnd="8dp">

            <TextView
                android:id="@+id/tvStopPointName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Bến xe Miền Đông"
                android:textColor="#212121"
                android:textSize="17sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvStopPointAddress"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="292 Đinh Bộ Lĩnh, P.26, Bình Thạnh, TP.HCM"
                android:textColor="#616161"
                android:textSize="14sp"
                android:layout_marginTop="4dp" />

            <TextView
                android:id="@+id/tvEstimatedTime"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Dự kiến: 07:30"
                android:textColor="#3F51B5"
                android:textSize="14sp"
                android:textStyle="italic"
                android:layout_marginTop="6dp" />
        </LinearLayout>

        <!-- CheckBox chọn -->
        <CheckBox
            android:id="@+id/cbSelect"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:focusable="false"
            android:focusableInTouchMode="false"/>
    </RelativeLayout>

</androidx.cardview.widget.CardView>
